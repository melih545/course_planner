re
pandas==1.5.3  # Replace with the version you're using, if known
streamlit==1.25.0  # Replace with the version you're using, if known
pyscipopt==4.3.0  # Replace with the version you're using, if known
matplotlib==3.8.0  # Replace with the version you're using, if known